/**
 * UI primitives used by both apps. Deliberately tiny — this is not a design
 * system, it is the short list of components that would otherwise have to be
 * duplicated across the public app and the Operations Center.
 *
 * Styling lives in `century-nit-core/styles/index.css`, which both apps import.
 */

export * from "./Sheet.js";
